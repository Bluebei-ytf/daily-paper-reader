---
title: "Graph Learning on Ensembles of Cyclic Peptides: An Investigation of Molecular Ensemble Modeling"
title_zh: 环肽系综上的图学习：分子系综建模研究
authors: "Aaron Feller, Kris Deibler, Maxim Secor"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21561v1"
tags: ["query:aichem-map"]
score: 9.0
evidence: 分子构象系综基础模型，在环肽上预训练
tldr: 传统分子性质预测通常采用单一构象，但许多分子以构象系综形式存在。本文提出EnsembleEGNN，先通过共享等变图网络编码每个构象，再用Set Attention块池化为整体嵌入，并在环肽系综数据集上以多任务自监督预训练。在CREMP-CycPeptMPDB上，预训练模型R²达0.477，优于仅序列的BERT；与BERT联合训练后进一步提升至0.538。该工作表明编码构象系综可改善环肽性质预测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21561v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1400, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21561v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 811, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21561v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 833, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21561v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 833, \"height\": 1012, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21561v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1213, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21561v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 857, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21561v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1703, \"height\": 1595, \"label\": \"Table\"}]"
motivation: 现有分子性质预测忽略构象系综，导致环肽性质预测精度不足。
method: 提出EnsembleEGNN，用共享EGNN编码各构象后以Set Attention池化，多任务自监督预训练。
result: 预训练模型R²=0.477，优于序列BERT的0.439；联合训练达0.538。
conclusion: 构象系综嵌入有效提升环肽性质预测，为分子建模提供新范式。
---

## 摘要
分子结构性质预测通常使用单个代表性构象，尽管许多分子在溶液中以构象系综形式存在。我们提出了EnsembleEGNN，一种分子系综基础模型，通过首先使用共享的等变图神经网络（EGNN）层对每个构象进行编码，然后使用Set Attention Block对得到的构象表示进行池化来编码系综。我们在环肽系综数据集CREMP上使用多任务自监督目标（结合掩码标记恢复、噪声坐标重建和成对距离重建）对模型进行预训练。在CREMP-CycPeptMPDB数据集上，从头训练EnsembleEGNN完全失败（R²=0.005）。然而，预训练模型达到了R²=0.477和皮尔逊r=0.699，优于仅序列的BERT基线（R²=0.439，皮尔逊r=0.667）。当EnsembleEGNN与BERT序列编码器进行端到端联合训练时，混合模型进一步提升至R²=0.538和皮尔逊r=0.737。这些结果表明，将构象系综编码为单个热力学信息嵌入可改善环肽性质预测。

## Abstract
Molecular property prediction from structure often uses a single representative conformation, even though many molecules exist as conformational ensembles in solution. We introduce EnsembleEGNN, a molecular ensemble foundation model that encodes an ensemble by first encoding each conformer with shared Equivariant Graph Neural Network (EGNN) layers, then pooling the resulting conformer representations with a Set Attention Block. We pretrain the model on CREMP, a cyclic peptide ensemble dataset, using a multi-task self-supervised objective combining masked token recovery, noisy-coordinate reconstruction, and pairwise distance reconstruction. On the CREMP-CycPeptMPDB dataset, training EnsembleEGNN from scratch fails entirely ($R^2=0.005$). However, the pretrained model reaches $R^2=0.477$ and Pearson $r=0.699$, outperforming the sequence-only BERT baseline ($R^2=0.439$, Pearson $r=0.667$). When EnsembleEGNN is co-trained end-to-end with the BERT sequence encoder, the hybrid model improves further to $R^2=0.538$ and Pearson $r=0.737$. These results demonstrate that encoding conformational ensembles into a single thermodynamically informed embedding improves cyclic-peptide property prediction.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：传统分子性质预测方法通常基于单一静态构象，但许多分子（尤其是环肽）在溶液中以波尔兹曼加权构象系综形式存在。忽略这种构象柔性导致性质预测精度不足。
- **研究背景**：
  - 图基础模型（GFM）在分子领域已取得进展，但存在“构象聚合问题”：结构方法处理单构象或简单池化，丢弃了热力学上下文。
  - 环肽具有固有柔性，其系综决定了膜渗透性、抗蛋白酶水解等关键性质，亟需能够编码整个构象系综的模型。

## 2. 论文提出的方法论

### 核心思想
- 构建一个**系综几何编码器**，将同一分子的多个构象（含波尔兹曼权重）编码为单一热力学信息嵌入，而非依赖单个构象。

### 关键技术细节

1. **数据表示**：
   - 每个分子包含原子类型 `a`、C 个构象的 3D 坐标 `X`、波尔兹曼权重 `wc`（取前 C=5 个并重新归一化）。
   - 输入形状：C × N_atoms × 3。

2. **构象级编码（共享 EGNN 层）**：
   - 每个构象独立通过 L 个等变图神经网络（EGNN）层，消息传递限于每个构象内的 k 近邻图。
   - 保证对旋转和平移的等变性，无需昂贵对齐或数据增强。

3. **构象融合（Set Attention Block + 波尔兹曼注意力）**：
   - 首先计算每个构象的平均节点嵌入 `¯h(c)`，然后通过可学习 MLP `f_ϕ` 结合对数波尔兹曼权重得到注意力权重 `α_c`（式 2）。
   - 利用 `α_c` 计算每个原子的加权平均嵌入 `hens_i`（原子级系综表示）。
   - 同时，将构象级嵌入通过 **Set Attention Block（SAB）** 获得全局图级嵌入 `zens`，SAB 使用 induced points 使复杂度从 O(N²) 降至 O(N·M)。

4. **多任务自监督预训练目标**（式 4）：
   - 掩码标记恢复（L_tok，交叉熵，权重 0.3）
   - 噪声坐标重建（L_coord，MSE，权重 0.5）
   - 成对距离匹配（L_dist，MSE，权重 0.2）：目标为波尔兹曼平均结构中的距离，而非平均距离。

### 算法流程（Algorithm 1）
- 输入原子类型、构象坐标、权重
- 每个构象经过 L 层 EGNN 更新
- 计算每个构象的平均嵌入，加上对数权重得到注意力分数
- softmax 得到注意力权重
- 生成原子级系综嵌入 `hens` 和全局嵌入 `zens`

## 3. 实验设计

### 数据集
- **CREMP**：环肽构象系综（无标签，用于预训练）
- **CREMP-CycPeptMPDB**：n=2979（边界过滤后），含环肽人工膜渗透性评分（log P_app），用于下游微调。

### Benchmark
- **5折交叉验证**：固定分割，3 折训练、1 折验证、1 折测试（测试集仅用于最终评估）。

### 对比方法
1. **BERT-only**：仅序列（PeptideCLM-2 编码器，114M 参数）
2. **EnsembleEGNN-random-init**：随机初始化几何模型（无预训练，11.3M 参数）
3. **EnsembleEGNN**：预训练几何模型
4. **Hybrid**：联合训练 BERT + EnsembleEGNN，拼接嵌入

## 4. 资源与算力

- **论文未明确说明使用的 GPU 型号、数量及训练时长。** 文中只提及模型参数量：EGNN 骨干 7.85M，构象融合 3.42M，总计约 11.3M（远小于 BERT 的 114M）。预训练 80 个 epoch，微调 20 个 epoch，batch size 8（EGNN）或 16（BERT）。未提供具体硬件信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：4 个模型（random-init、BERT-only、EnsembleEGNN、Hybrid）进行 5 折交叉验证，每折 3 次独立重复 → 共约 15 个训练运行（微调）。
  - 预训练阶段一次（80 epoch，记录 loss）。
  - 未进行专门的消融实验（如不同预训练任务权重、不同构象数量 C）。

- **充分性与公平性**：
  - 使用相同数据分割和微调协议，对比条件公平。
  - 随机初始化模型表现极差（R²=0.005），明确证明预训练必要性，实验设计合理。
  - 但仅在一个中等规模数据集（n≈3000）上评估，未在其他分子体系（如蛋白质、RNA）或更大数据集上验证泛化性，覆盖广度有限。

## 6. 论文的主要结论与发现

- **预训练至关重要**：未预训练的 EnsembeEGNN 几乎随机（R²≈0），而预训练后 R²=0.477，超过 BERT-only（0.439）。
- **混合模型最佳**：联合 BERT 序列编码器后 R² 提升至 0.538，Pearson r 0.737，MAE 和 RMSE 均下降。
- **系综编码有效**：直接利用构象系综（而非单构象）可显著提升环肽性质预测。
- **参数效率**：11.3M 参数的几何模型优于 114M 的序列模型。

## 7. 优点

- **创新性方法**：提出“系综几何基础模型”范式，将热力学权重显式融入注意力机制，区别于以往单构象或朴素池化。
- **等变性与可扩展性**：EGNN 保证旋转平移等变性；Set Attention Block 使复杂度线性于构象数，支持更大系综。
- **多任务预训练**：联合掩码、去噪、距离三个目标，迫使模型学习构象系综内部的几何与语义。
- **实验设计严谨**：5 折交叉验证 + 独立三次重复，报告均值和标准误；明确区分预训练与微调阶段。
- **开源**：公开代码、预训练权重，可复现。

## 8. 不足与局限

- **构象交互有限**：消息传递仅在单个构象内进行，缺乏跨构象的信息交换，池化后才融合。
- **数据限制**：仅使用 CREMP 预计算系综（C=5），质量受限于原有数据集；仅一个下游任务（膜渗透性），样本量较小（n=2979），跨论文比较需谨慎。
- **缺乏消融研究**：未系统分析不同预训练目标、构象数量 C、k 近邻大小等超参数的影响。
- **算力信息缺失**：未报告 GPU 型号、训练耗时，复现者难以估计资源需求。
- **泛化性未知**：未验证到其他分子类型（如蛋白质环、小分子）或更复杂的系综（如多态性 RNA），结论的普适性受限。

（完）
